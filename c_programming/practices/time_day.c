// Rv 7th Time of day
#include <stdio.h>
#include <string.h>
int main (void){
    int time;
    char name[50];
    printf("Ayo, What time is it?:");
    scanf("%d",&time);

    return 0;
}
    if(time >= 13)

    return 0;
   
    printf("its afternoon;");
     
    else if(time <= 10)
     printf("it's still so early");
     
     else if(time <= 12){
     printf("it's now evening");
     }